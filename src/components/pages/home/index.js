import React from "react";
import ContainerLayout from "../../layouts/container";
import styles from "./style.sass";

const HomePage = () => (
  <ContainerLayout>
    <h1 className={styles.title}>{"GOOOOOD MOOORNING, JESS!!!!"}</h1>
  </ContainerLayout>
);

export default HomePage;
